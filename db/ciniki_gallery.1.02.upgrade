#
# This file will add the uuid to ciniki_events
# which is required for the sync to work.
#
ALTER TABLE ciniki_gallery ADD COLUMN album_id int not null AFTER album;
ALTER TABLE ciniki_gallery COMMENT='v1.02';
